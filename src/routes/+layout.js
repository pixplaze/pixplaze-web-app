// Отключаем SSR по умолчанию для всех маршрутов: приложение рендерится только
// в браузере (SPA-режим), поэтому браузерные API (localStorage и т.п.) доступны
// везде и не падают на сервере.
//
// Это значение наследуется всеми вложенными маршрутами. Любой из них может
// переопределить поведение у себя, объявив `export const ssr = true;`
// в собственном +layout.js / +page.js.
export const ssr = false;
